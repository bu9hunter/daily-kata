using System;

namespace WordWrap
{
    public class WordWrapper
    {
        public WordWrapper()
        {
        }

        public string wrap(string inputText, int cutLength)
        {
            if (inputText.Length < cutLength)
            {
                return inputText;
            }

            

            return inputText;
        }
    }
}